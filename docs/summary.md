# Lab Summary

Answer each question in full sentences. Each answer starts as a placeholder marker. Replace
every marker with your own writing, and delete the marker itself. The minimum word count for
this document is `150` words.

> Stage One loops over `range(1, flashes + 1)`. Say what `range(flashes)` would hand the loop
> instead, and what the printed flash numbers would look like if you used it.

TODO

> Stage Two's `while` loop stops on its own. Name the line inside the loop that makes it stop,
> explain what the program would do without that line, and say how many trips a reading of
> `20` makes and why.

TODO

> Stage Four is a loop inside a loop. For `2` repeats, how many times does the lantern come on,
> and how did you count it? Then explain what would change if the three letter loops sat after
> the outer loop instead of inside it.

TODO
